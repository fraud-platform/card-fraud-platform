services:
  rule-engine-auth:
    environment:
      - APP_ENV=local
      - SECURITY_SKIP_JWT_VALIDATION=true
      - QUARKUS_SMALLRYE_JWT_ENABLED=false
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 2g

  rule-engine-monitoring:
    environment:
      - APP_ENV=local
      - SECURITY_SKIP_JWT_VALIDATION=true
      - QUARKUS_SMALLRYE_JWT_ENABLED=false
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 2g